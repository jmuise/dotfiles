# apt packages — Ubuntu/Debian
# Install: xargs sudo apt-get install -y < packages/apt.txt
# (One package per line; lines starting with # are stripped by xargs)

# Shell
zsh
bash
tmux

# Modern CLI
bat
ripgrep
fd-find
fzf
jq

# Dev
git
gh
git-delta
curl
wget
build-essential
direnv
unzip

# Node.js — provides npm, needed for Kilo Code CLI install (npm install -g @kilocode/cli)
# and GitHub Copilot CLI install (npm install -g @github/copilot)
nodejs
npm

# Containers
docker.io
docker-compose
# docker-compose already installs its plugin to /usr/libexec/docker/cli-plugins
# (one of docker's built-in plugin search dirs), but docker-buildx doesn't
# ride along with anything above - install it explicitly so `docker build`'s
# BuildKit path and `docker compose build` work without depending on Docker
# Desktop's WSL integration (whose /mnt/wsl/docker-desktop/cli-tools mount
# goes stale across Docker Desktop VM restarts - see wsl/.wslconfig.template).
docker-buildx
