# winget packages — Windows (system-level installs that need OS integration)
# Developer tools have moved to packages/scoop.txt.
# PowerShell 7 and Python are bootstrapped earlier in install.ps1 via winget
# before Scoop is available on a fresh machine.

# Containers — needs WSL2 kernel integration, not suitable for Scoop
Docker.DockerDesktop
