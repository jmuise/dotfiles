# Pinned for `make roster` / `make roster-check` in CI. Jinja2 also ships
# bundled with Ansible (see roster/render.py's docstring) — that copy is fine
# for a quick local check, but CI installs this pin explicitly rather than
# depending on whatever Ansible happens to vendor.
jinja2==3.1.6
