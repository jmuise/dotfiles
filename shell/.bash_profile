# .bash_profile — bash login shell config
# macOS terminal opens login shells by default, so this is the entry point.
# We just source .bashrc to keep everything in one place.

[[ -f "$HOME/.bashrc" ]] && source "$HOME/.bashrc"
